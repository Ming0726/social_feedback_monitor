# 关键词过滤机制说明

> 用于精准抓取「微信面对面红包」相关用户反馈，同时识别「微信 vs 竞品」对比型内容

---

## 1. 核心理念

「面对面红包」「扫码红包」这类词在中文互联网信号噪音比极差：
- 支付宝、招商银行 App、云闪付都有"面对面红包"
- 抖音/快手/拼多多/美团遍地"扫码红包活动"
- B站到处是"红包封面教程"
- 灌水评论"111""233""兄弟在吗"

**简单搜「面对面红包」，10 条结果里 8 条是噪音。**

本机制的核心理念分两块：

### 1.1 三分类思路

不是"二选一"（要么收、要么丢），而是把内容分三类：

| 分类 | 含义 | 处理 |
|---|---|---|
| `feedback` | 纯微信反馈 | 入库 |
| `competitor_compare` | 微信 + 竞品对比 | **入库（高价值）** |
| 噪音 | 纯竞品 / 不相关 | 丢 |

> 用户拿微信和支付宝/云闪付对比的内容，是产品分析最关心的——因为它直接告诉你"用户脑子里的真实对标和差距在哪"。

### 1.2 召回宽 + 过滤严

```
搜索阶段 ━━━━━━━━━━━━━━━━━━━━┓  尽可能多召回，把决策留给后端
                              ┃
入库前 ━━━ 多道闸层层筛选 ━━━━━┛  只让真正有价值的进库
```

---

## 2. 关键词分类（在 `config.py`）

### 2.1 用于"识别微信场景"的关键词

#### STRONG_KEYWORDS — 强关键词（命中即视为微信）

```python
STRONG_KEYWORDS = [
    "微信面对面红包",
    "微信 面对面红包",
    "微信扫码红包",
    "微信 扫码红包",
    "微信二维码红包",
    "面对面红包打不开",
    "面对面红包扫不出",
    "面对面红包扫不了",
    "面对面红包用不了",
]
```

**设计逻辑**：
- 前 5 个：自带"微信"二字，从源头规避歧义
- 后 4 个：「问题描述型」短语——"面对面红包 + 打不开/扫不出/扫不了/用不了"几乎只在微信场景出现（其他厂商叫法完全不同）

#### WEAK_KEYWORDS — 弱关键词（必须搭配"微信"上下文）

```python
WEAK_KEYWORDS = [
    "面对面发红包",
    "面对面收红包",
]
```

这两个词是微信菜单**原话**。但其他场景（招行 App 也叫这个）会有少量误用，所以保险起见要求文本里同时出现"微信"。

#### CONTEXT_WORDS — 上下文词（弱词的"通行证"）

```python
CONTEXT_WORDS = ["微信", "WeChat", "wechat", "wx", "WX"]
```

任意一个出现 = 微信场景。

### 2.2 用于"识别竞品对比"的关键词

#### COMPETITORS_DIRECT — 直接竞品（产品对位竞争）

```python
COMPETITORS_DIRECT = [
    "支付宝",
    "云闪付",
    "数字人民币",
    "招商银行", "招行",
    "QQ红包", "qq红包",
    "翼支付",
    "京东支付", "京东钱包",
]
```

这些是**移动支付/红包产品的直接对标**。出现这类词时，"微信 vs X"的对比尤其有产品分析价值。

#### COMPETITORS_OTHER — 间接竞品（场景型）

```python
COMPETITORS_OTHER = [
    "抖音红包", "抖音活动",
    "快手红包",
    "拼多多",
    "美团红包",
    "滴滴红包",
    "饿了么红包",
    "京东红包",
    "百度红包",
    "淘宝", "天猫",
]
```

红包场景里频繁出现，但不是直接对标产品（多半是促销活动）。也会标成 `competitor_compare`，但产品价值低于直接竞品。

### 2.3 兜底排除词

```python
EXCLUDE_KEYWORDS_IF_ONLY = [
    "群红包",
    "拼手气红包",
    "口令红包",
    "封面红包",
]
```

如果命中的关键词**全部**在这个列表里 → 丢（防止"微信封面红包教程"这种不相关内容混进来）。

---

## 3. 搜索阶段（怎么用关键词去各平台搜）

```python
KEYWORDS = STRONG_KEYWORDS + WEAK_KEYWORDS
```

**11 个关键词全部拿去搜**——召回是搜索引擎的事，过滤是入库前的事。
不要在搜索阶段就严格过滤，否则会漏掉很多潜在反馈。

---

## 4. 入库前的过滤流程（六道闸）

写在 `crawlers/base.py` 的 `match_keywords()` 函数里。

```
                ┌──────────────────────────────────┐
                │   候选文本（笔记正文 / 评论）       │
                └────────────────┬─────────────────┘
                                 ▼
        【闸 1】扫描文本里命中了哪些 STRONG / WEAK 关键词
                一个都没命中 → 丢
                                 │
                                 ▼
        【闸 2】扫描文本里出现了哪些竞品（DIRECT + OTHER）
                                 │
            ┌────────────────────┴────────────────────┐
            │                                         │
        含竞品                                    不含竞品
            │                                         │
            ▼                                         ▼
        【闸 3a】文本里有"微信/WeChat/wx"吗?    【闸 3b】是否只命中弱词?
            │                                         │
        ┌───┴───┐                              ┌──────┴──────┐
        是      否                              是             否
        │       │                              │              │
        ▼       ▼                              ▼              ▼
    收录       丢            是否含"微信"上下文?            收录
    ↓     (纯竞品)               │                          ↓
category=                    ┌───┴───┐                  category=
"competitor_                  是      否                  "feedback"
 compare"                    │       │
                             ▼       ▼
                         收录       丢
                          ↓       (无微信)
                       category=
                       "feedback"
                                 │
                                 ▼
        【闸 4】(子类可定制) SPAM 词（如 B站的"红包封面/教程"）
                命中 → 丢
                                 │
                                 ▼
        【闸 5】(子类可定制) 太短的灌水评论（< 8/12 字）→ 丢
                                 │
                                 ▼
        【闸 6】兜底：命中的关键词全是普通红包类型词（"群红包"等）→ 丢
                                 │
                                 ▼
                            ✅ 入库
                            写入字段：
                            - matched_keyword       命中的关键词
                            - category              feedback / competitor_compare
                            - mentioned_competitors 提到的竞品名（| 分隔）
```

### 闸的职责一句话总结

| 闸 | 职责 | 实现位置 |
|---|---|---|
| 闸 1 | 没命中关键词 → 丢 | `base.py: match_keywords()` |
| 闸 2 | 扫描竞品词 | `base.py: match_keywords()` |
| 闸 3a/3b | 决定 `feedback` 还是 `competitor_compare` 还是丢 | `base.py: match_keywords()` |
| 闸 4 | 平台特定 SPAM 词 | `crawlers/bilibili.py: SPAM_KEYWORDS` |
| 闸 5 | 灌水评论太短 | 各爬虫里 `len(text) < 8` |
| 闸 6 | 兜底：命中词都是普通红包词 | `base.py: match_keywords()` |

---

## 5. 评论的特殊处理：上下文判定

评论本身可能很短，比如"以前的设计更好"——单独看不一定能命中关键词。

**解决方案**：把笔记/视频的标题+正文当作评论的"上下文"，拼起来一起判断：

```python
# 在 xiaohongshu.py 里
joined = f"{full_text}\n{ctext}"   # 笔记正文 + 评论
cls_c = self.classify(joined)
```

这样就能避免把"评论本身没说微信，但所在笔记是讲微信的"这种高价值内容误杀。

---

## 6. 数据库字段（`raw_feedback` 表）

新机制涉及 3 个核心字段：

| 字段名 | 类型 | 含义 | 示例 |
|---|---|---|---|
| `matched_keyword` | TEXT | 命中的关键词（多个用 \| 分隔） | `微信面对面红包\|面对面发红包` |
| `category` | TEXT | 分类 | `feedback` / `competitor_compare` |
| `mentioned_competitors` | TEXT | 提到的竞品（\| 分隔） | `支付宝\|云闪付` |

> 旧数据库**自动迁移**——程序启动时检测到没这两个字段会自动添加，旧数据的 `category` 默认填 `feedback`，不会丢数据。

---

## 7. 实际效果对比

| 文本 | 旧逻辑（v1）| 新逻辑（v2）|
|---|---|---|
| "微信面对面红包真烂" | ✅ 收（feedback）| ✅ 收（feedback）|
| "支付宝面对面红包活动" | ❌ 丢 | ❌ 丢（没微信）|
| "还好有云闪付可以发，微信面对面发红包没了" | ❌ 丢（被云闪付排除） | ✅ **收（竞品对比，提到云闪付）** |
| "支付宝的扫码红包比微信好用" | ❌ 丢 | ✅ **收（竞品对比，提到支付宝）** |
| "招行 App 有面对面发红包" | ❌ 丢 | ✅ **收（竞品对比，提到招商银行）** |
| "今年开工不准备纸红包了，微信不行就试试云闪付和翼支付" | ❌ 丢 | ✅ **收（竞品=云闪付\|翼支付）** |
| "支付宝活动来扫码领红包"（无微信） | ❌ 丢 | ❌ 丢 |
| "群红包被秒抢" | ❌ 丢 | ❌ 丢（兜底闸 6） |
| "111"（评论）| ❌ 丢 | ❌ 丢（闸 5） |

---

## 8. 配置位置一览（你以后想调整就改这里）

| 想调什么 | 改哪里 | 改完什么时候生效 |
|---|---|---|
| 加/减微信关键词 | `config.py` 的 `STRONG_KEYWORDS` `WEAK_KEYWORDS` | 下次跑爬虫立即生效 |
| 新增直接竞品 | `config.py` 的 `COMPETITORS_DIRECT` | 同上 |
| 新增间接竞品 | `config.py` 的 `COMPETITORS_OTHER` | 同上 |
| "微信"同义词 | `config.py` 的 `CONTEXT_WORDS` | 同上 |
| B站特有营销词排除 | `crawlers/bilibili.py` 的 `SPAM_KEYWORDS` | 同上 |
| 评论最短字数 | 各爬虫里的 `len(...) < 8` | 同上 |

---

## 9. 真实案例分析

跑过的一次小红书数据（18 条入库）：

| 内容 | 命中关键词 | 应分类 |
|---|---|---|
| "微信面对面收红包变了体验太差了..." | 微信面对面红包\|面对面收红包 | feedback |
| "微信面对面红包没了... 支付宝也是的，一直在推你的碰一碰..." | 微信面对面红包 | **competitor_compare**（提到支付宝）|
| "云闪付救广东打工仔狗命！... 微信面对面发红包功能取消了" | 微信面对面红包\|面对面发红包 | **competitor_compare**（提到云闪付）|
| "招行 app 有面对面发红包"（评论） | 面对面发红包 | **competitor_compare**（提到招商银行）|

---

## 10. 数据查询常用命令

```bash
# 看竞品对比类的所有数据
sqlite3 -column -header data/data.db "SELECT publish_time, mentioned_competitors, substr(content,1,150) FROM raw_feedback WHERE category='competitor_compare' ORDER BY publish_time DESC"

# 哪些竞品被提到最多
sqlite3 -column -header data/data.db "SELECT mentioned_competitors AS 竞品, COUNT(*) AS 次数 FROM raw_feedback WHERE category='competitor_compare' GROUP BY mentioned_competitors ORDER BY 次数 DESC"

# 按分类统计总数
sqlite3 -column -header data/data.db "SELECT category, COUNT(*) FROM raw_feedback GROUP BY category"

# 看某个具体竞品的所有对比
sqlite3 -column -header data/data.db "SELECT publish_time, substr(content,1,200) FROM raw_feedback WHERE mentioned_competitors LIKE '%支付宝%'"
```

---

## 11. 这套机制的两大代价

### 代价 1：召回率仍有损失

弱词（"面对面发红包/收红包"）不带"微信"上下文且不带任何竞品时会被过滤掉。
**少数纯吐槽性反馈可能漏抓**——比如"以前那个红包扫一扫真好用，现在搞复杂了"，没出现"微信"也没出现强词。

但优先保证**准确率 > 召回率**：
- 18 条高质量数据 >> 200 条 80% 噪音的数据。
- 后者根本没法用。

### 代价 2：维护成本

随着时间推移，会发现：
- 新的产品也叫"面对面红包"（如某新出的银行 App）→ 加到 `COMPETITORS_DIRECT`
- 新的促销活动同名 → 加到 `EXCLUDE_KEYWORDS_IF_ONLY` 或 `COMPETITORS_OTHER`
- 新的"微信"称呼（如某种黑话）→ 加到 `CONTEXT_WORDS`

但维护方式很简单：**`config.py` 里加一行就行，不用改任何爬虫代码**。

---

## 12. 一句话总结

> **不靠"白名单越严越好"，靠"白名单 + 上下文判定 + 竞品分类"三件武器组合**：
> - **白名单**（强词）从源头限定语义
> - **上下文判定**（笔记+评论联合判断、CONTEXT_WORDS 兜底）避免误杀
> - **竞品分类**把"竞品对比"从噪音里抢救出来变成黄金数据
